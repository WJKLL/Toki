# PLAN · 空间图片 组件体系与动态导出 v2.0

> 编号规划：组件 **C-7x** / 服务 **S-4x** / 工具 **U-1x**
> 前置：`PLAN_wallpaper_v1.52.md`（渲染管线）、`PLAN_components_v1.53.md`（期 1~6 分期）
> 本文档覆盖两件新事：**① 动态图导出**、**② 组件体系（含现代化样式）**

---

## 一、现状整理（截至 `b968162`）

### 1.1 已经跑通、**不要动**的部分

| 子系统 | 状态 | 关键决定 / 数据 |
|:---|:---|:---|
| **深度模型** | ✅ | Depth Anything V2 Small **int8（27 MB）** 替换 YOLO26-n。换型理由：风景图前后识别（实测富士山那张 YOLO26 上下半差 −0.120 方向反了，DAV2 +0.010 正确） |
| **主体分割** | ✅ | modnet(fp32) + isnet-anime(int8) 双模型；**用 isnet 覆盖率仲裁**（≥8% 用 isnet，否则退回 modnet） |
| **分层** | ✅ | 有 mask → 主体层（mask 界定边界）+ 深度层；主体区域深度**压平到层心**避免被拆散 |
| **渲染** | ✅ | `layer_compose.frag`；同向递减（远 1.0 → 主体 0.5）；`uZoom` 防露底；背景**边缘外推**消除轮廓色差 |
| **路径分流** | ✅ | 用户选 **自动 / 人物 / 风景**（自动判据实测 4 个指标全不可分，故交给人） |
| **晃动输入** | ✅ | S-41：**传感器（加速度计倾斜）/ 摇杆 / 自动轨迹** 三档 + 零位校准 |
| **手动修正** | ✅ | U-14 涂刷，**可选刷到第几层**（改深度到层心，渲染侧零改动） |
| **历史** | ✅ | S-40 草稿箱：导出 / 删除 / 20 条上限自动清理 / 资源随删除释放 |
| **UI** | ✅ | 澎湃风：三级菜单 + 面板互斥 + 逐级返回 + 过渡动画 + 双指缩放 |
| **命名** | ✅ | 叫**空间图片**（不接系统壁纸 API） |

### 1.2 已知待补（小项，先做完再上大功能）

- [ ] 工具块固定 68×68 / 纯黑底 `#000` / 三级标签字号
- [ ] 刻度尺滑卡（细密刻线 + 当前刻线拉长变金）
- [ ] 顶栏补按钮：**重做 ↷**（笔刷加 redo 栈）、**对比 ◁\|▷**（按住看原图）、**✓**
- [ ] 组件页二级拆分（添加 / 参数）

---

## 二、动态图导出（P0）

### 2.1 为什么它是当前最该做的

用户目标原话是"**单纯手机晃动，人物主体有点立体感**"——**这是一个动态效果**。
静态 PNG 只能截其中一帧，**表达不了空间感**。所以动图不是锦上添花，是**这个效果的正确载体**。

### 2.2 格式选择：**GIF 优先**

| 格式 | 依赖 | 体积 | 微信可发 | 结论 |
|:---|:---|:---|:---|:---|
| **GIF** | **纯 Dart 可写（LZW）**，零平台依赖，鸿蒙同样可用 | 大（256 色） | ✅ | **先做这个** |
| MP4 | 系统 MediaCodec（Android）/ 其他平台各写一套 | 小 | ✅ | 期 2 |
| WebM / APNG | 平台差异大 | — | ⚠️ | 不做 |

> ⚠️ 沿用既有决策：**不引入 ffmpeg**（体积 + 鸿蒙移植）。

### 2.3 关键架构：**把"晃动来源"抽象成统一接口**

现在三档输入（传感器 / 摇杆 / 自动）已经统一输出 `Offset(-1..1)`。
导出只需要**再加一档**：

```
SpatialInputSource（S-41 已落地）
  ├── 传感器（加速度计）      ✅
  ├── 摇杆                    ✅
  ├── 自动正弦轨迹            ✅
  └── ★ RecordingInputSource  ← 新增：按时间返回预设轨迹，与实时同构
```

**好处**：导出走的是**同一套渲染代码**，不另写一条管线 —— 所见即所得。

### 2.4 逐帧渲染与编码

```
for (t in 0..duration) {
  shift = recording.shiftAt(t)          // 程序化轨迹
  await 下一帧                            // 等 LayeredParallaxView 重绘
  ui.Image frame = await boundary.toImage(pixelRatio: …)
  gif.addFrame(frame)                    // ★ 立刻转成索引色后 dispose
}
gif.finish() → 保存到相册（复用 PlatFileOps.saveImageToGallery）
```

**必须守住的约束**：

| 约束 | 数值 / 做法 | 原因 |
|:---|:---|:---|
| 分辨率 | 720p（可选中/高） | GIF 体积随分辨率平方涨 |
| 帧率 | **20~24 fps** | GIF 刷新率本来就低，30 帧只是白增体积 |
| 时长 | **3s** 默认（可选 3/5/10） | 空间感的观看集中在开头 1.5s |
| 内存 | **逐帧编码，转完立刻 dispose** | 72 帧 × 720p RGBA ≈ 250 MB，攒着必 OOM |
| 调色板 | 全局 256 色，从**第一帧**取 | 逐帧调色板会闪烁 |

**验收**：5s@720p 导出 < 30s、内存峰值 < 500 MB、能直接发微信。

---

## 三、组件体系（P1，本次重点）

### 3.1 现状为什么"太 low"

当前只有 `SpatialComponent.clock` 一个数字时钟，**样式硬编码**：没有字体、没有颜色体系、没有导入、没有预设。
根因不是"少做了几个组件"，而是**没有样式层** —— 加十个组件也只是十个硬编码。

### 3.2 分两层：**组件（是什么）× 样式（长什么样）**

```
Component          类型 + 内容 + 空间位置
  ├─ kind          时间 / 文字 / 图象 / 表情 / 环境
  ├─ content       各自的内容（格式串 / 文本 / 图片 / 位置名…）
  ├─ u, v, scale   画面位置与缩放
  ├─ depth         绑定到场景的哪个深度平面   ← 决定视差与遮挡
  └─ tiltX, tiltY  3D 透视
Style              外观（★ 这套要能复用到所有组件）
  ├─ 字体  family / weight / letterSpacing / 行高
  ├─ 颜色  纯色 或 渐变（角度 + 色标）
  ├─ 描边  width / color
  ├─ 阴影  blur / offset / color
  ├─ 发光  color / radius
  ├─ 玻璃  blur / tint / 边框高光
  └─ 圆角 / 内边距
```

**样式预设**（先做 5 套，覆盖绝大多数审美）：
`极简` · `玻璃` · `霓虹` · `手账` · `描边字`

### 3.3 组件清单（按优先级）

| 期 | 组件 | kind | 说明 |
|:---|:---|:---|:---|
| **P1** | **艺术字** | 文字 | 自定义文本 + 字体 + 渐变/描边/发光。**投入产出比最高** —— 一套文字渲染 + 样式层，直接撑起"现代化"观感 |
| **P1** | **时钟** | 时间 | 数字钟（样式化）+ 指针钟 + 翻页钟 |
| **P1** | **Logo / 贴纸** | 图象 | PNG/SVG 导入 + 内置图标集 |
| **P2** | **表情** | 表情 | emoji / 颜文字 / 内置表情包；可做**轻微摆动**（比时钟更有"活气"） |
| **P2** | **日期 / 倒计时 / 纪念日** | 时间 | 复用时间组件 |
| **P2** | **位置** | 环境 | 城市名（可手动填，不必联网；联网定位另开开关） |
| **P2** | **天气** | 环境 | 温度 + 图标（**手动填优先**，联网另开） |
| **P3** | **心情 / 标签** | 环境 | 胶囊标签、手写风便签 |

> 「城市 / 天气」默认**不联网**：一是网络权限与隐私，二是断电/无网时的确定性。
> 联网版本作为可选开关，单独一期做。

### 3.4 与景深的配合（这是我们相对"贴纸 App"的核心优势）

| 能力 | 做法 | 期 |
|:---|:---|:---|
| **视差** | 组件位移 = `depth` 对应的层位移 × `parallax` 系数；**默认时钟系数 0（固定）** | 已有雏形 |
| **3D 透视** | `tiltX/tiltY` 跟晃动做透视旋转 → 像"贴在场景里的贴纸"而不是浮在屏幕上 | P1 |
| **遮挡** | 组件按 `depth` 与其他图层**一起排序**渲染（钟被树枝挡住） | P2 |
| **融图** | ① 玻璃材质采样**背景色**做 tint；② 阴影方向跟场景光一致 | P2 |
| **动态图里融入** | 组件与画面**共用同一个 shift**（已实现）→ 导出动图时天然同步 | P1 |

### 3.5 编辑交互（对标澎湃/系统相册的成熟做法）

- 单指拖：移动（已实现）
- **双指垂直滑**：连续调 `depth`（比滑块直观）
- **双指旋转**：调 `tiltX/tiltY`
- 双指捏合：缩放
- 长按拖出画布：删除
- 选中后出现**样式面板**（字体 / 颜色 / 描边 / 预设）

---

## 四、分期与验收

| 期 | 内容 | 验收 |
|:---|:---|:---|
| **0** | 补完 1.2 的小项（块尺寸/滑卡/按钮/二级拆分） | 视觉收口，无遗留小坑 |
| **1** | **艺术字 + 样式层 + 样式预设** | 同一个"你好"，能变出 5 套明显不同的风格 |
| **2** | **时钟三式（数字/指针/翻页）** | 全部走样式层，可换字体颜色 |
| **3** | **Logo/贴纸导入**（PNG 先，SVG 后） | 导入一张 PNG 能贴上去并调深度 |
| **4** | **动态图导出（GIF）** | 3s@720p 能导出、能发微信；5s 导出 < 30s |
| **5** | 表情 / 城市 / 天气 / 心情 | 手动填为主，联网可选 |
| **6** | 组件遮挡 + 玻璃融图 | 组件能被前景遮住 |
| **7** | MP4 导出 | 体积比 GIF 小一个量级 |

> **顺序理由**：期 1~3 先把"组件+样式"的地基打好，期 4 的动图才有内容可动。
> 反过来先做导出，导出的只是一张会晃的空图片。

---

## 五、技术风险

| 风险 | 对策 |
|:---|:---|
| GIF 体积失控 | 全局调色板 + 720p + 20fps；提供"高/中/低"三档 |
| 导出 OOM | **逐帧编码，转完立刻 dispose**，绝不攒帧 |
| 字体导入（.ttf/.otf） | Flutter 运行时加载字体需要 `FontLoader`；**鸿蒙侧要验证** |
| SVG 渲染 | 需要 `flutter_svg`（平台无关，风险低） |
| 组件与涂刷手势冲突 | 组件只在"组件"页可拖；其它页手势让位（已在涂刷上踩过，同一套做法） |
| 样式层膨胀 | `Style` 用不可变值对象 + `copyWith`，预设就是几个常量 |

---

## 六、下一步（本次要开工的）

1. **期 0**：补完 1.2 的 4 个小项（半天）
2. **期 1**：艺术字 + 样式层（**这是"现代化"的关键一步**）

**建议先做期 1 的样式层骨架** —— 它一旦立起来，时钟/Logo/表情/天气全都受益；
反过来每加一个组件都硬编码一次，就还是会回到"太 low"。

---

## 七、镜头运动预设（纳入期 4，与动态导出同期）

### 7.1 架构上已就绪

S-41 的三档输入（传感器 / 摇杆 / 自动正弦）**统一输出 `Offset(-1..1)`**。
导出只要再加一档 **`CameraPreset`** —— 按时间返回预设轨迹，与实时同构：

```
SpatialInputSource
  ├── 传感器（加速度计）   ✅
  ├── 摇杆                 ✅
  ├── 自动正弦             ✅
  └── ★ CameraPreset       ← 期 4 新增
```

**每个预设不是新代码，只是一组参数**：

```
CameraPreset { 名字 · 时长 · 是否循环 · shift(t) → Offset }
```

⇒ **导出与预览共用同一条渲染管线，必然一致**，不会出现"预览好看、导出跑偏"。

### 7.2 预设清单

| 预设 | 轨迹 | 观感 | 无缝循环 |
|:---|:---|:---|:---|
| **微晃** | 双轴异频正弦（现有） | 最安全，像手持 | ✅ 天然 |
| **横摇 Pan** | 左→右单向 | 像摇头看房间 | ⚠️ 需做成往返 |
| **俯仰 Tilt** | 上→下 | 像上下打量 | ⚠️ 需做成往返 |
| **推进 / 拉远** | 驱动 `uZoom` 做轻微缩放 | 像凑近看 | ✅ 往返 |
| **环绕 Orbit** | 左右 + 前后合成椭圆 | **最有 3D 感** | ✅ 天然 |
| **手持感** | 低频噪声 + 偶发微抖 | 真实但不晕 | ❌ 噪声不闭合 |
| **回正** | 从偏移回到居中 | 视频收尾 | — |

### 7.3 四条必须提前定的约束（决定"能不能做"）

| # | 约束 | 说明 |
|:---|:---|:---|
| **1** | **循环闭合** | 动图要能无缝循环 ⇒ **第 0 帧 == 最后一帧**。正弦/环绕天然闭合；单向 Pan/Tilt 不闭合，需往返或不做循环 |
| **2** | **幅度与穿帮** | 位移越大、边缘露出越多。预设要**按幅度分级**（轻/中/强），并让 `uZoom` **按该预设的最大位移自动配** |
| **3** | **周期整除时长** | 轨迹周期要能整除导出时长，否则循环处会跳 |
| **4** | **方向符号** | "向右摇"是画面左移还是右移 —— **观感与实现的符号最容易返工**，一次定义清楚 |

### 7.4 先做哪三个

`微晃`（默认）· `横摇`（最像"看照片"）· `环绕`（最有空间感）
—— 覆盖「最安全 / 最直观 / 最有 3D 感」，且**都能无缝循环**，卡在约束 1 的安全侧。

### 7.5 加分项：预设 × 景深联动

例如"推进"时让**焦点深度也随时间缓慢前移** ⇒ 前后景的移动比例随时间变化，
观感比固定视差强一个档次。实现上只是**把 `_focus` 也做成时间的函数**，工作量很小。

---

## 八、玻璃 / 折射作用在【字与图案本身】（期 2）

### 8.1 目标

不是给组件垫一个玻璃方块，而是**让数字、字、Logo 自己变成玻璃**：
背后的画面透过笔画发生**模糊与折射**，像真的玻璃字。

### 8.2 技术路径（不需要新造轮子）

```
ShaderMask(                 // ① 用【字形】当遮罩（srcIn 只保留文字形状）
  blendMode: BlendMode.srcIn,
  shaderCallback: (b) => 把文字渲染成 shader,
  child: BackdropFilter(    // ② 采样它【背后】的画面
    filter: ImageFilter.shader(折射 shader),   // ③ 复用已有的 lens_refraction.frag
    child: SizedBox.expand(),
  ),
)
```

- `BackdropFilter` 采样的是**已经画在它下面的东西** ⇒ 天然就是"透过字看见背景"
- `ImageFilter.shader(FragmentShader)` 让我们能塞**自定义折射**，而不只是模糊
- 折射 shader 我们**已经有了**（`lens_refraction.frag`，光感水印模块在用）⇒ 直接复用

### 8.3 三个必须处理的点

| # | 问题 | 对策 |
|:---|:---|:---|
| 1 | **性能** | BackdropFilter 会强制图层分离、且读回后台缓冲。**只在组件区域生效**（不是全屏），并且**只在真的开了玻璃时才挂**（`glass > 0` 才 build 这一层） |
| 2 | **导出一致性** | 离屏 `toImage` 路径下 BackdropFilter 行为需实测；若不一致，导出时降级为"预烘焙的折射贴图" |
| 3 | **与外壳的关系** | 这是**两条不同的路**：<br>· `glass` 滑条 = 给组件垫玻璃底（卡片感）<br>· **本方案 = 让内容自己成为玻璃**<br>UI 上要分开命名，别让用户以为是同一个东西 |

### 8.4 落地顺序

先做**模糊版**（`ImageFilter.blur`，稳），确认导出一致后再换**折射版**
（`ImageFilter.shader` + `lens_refraction.frag`）—— 分两步走，出问题好定位。

### 8.5 具体实现（C-70 `SpatialGlassText`）

```
① 用 TextPainter 把文字画进一张 ui.Image（只用它的 alpha）→ 形状图
② ShaderMask(blendMode: dstIn)  + ImageShader(形状图)  → 只保留笔画
③ 遮罩里放 BackdropFilter —— 它采样的是【已经画在它下面的画面】
```

⇒ "透过笔画看见被模糊的背景"自然成立，**不需要改渲染管线**。

两个必须处理的细节：
- **形状图异步渲染**：BackdropFilter 要先知道边界，而"文字的 alpha"只有画出来才知道。
  首帧形状图没好时返回等尺寸空盒（一帧，肉眼不可见）。
- **形状图必须缓存**：每次 build 重画文字 + `toImage`（会走 GPU 回读）是纯浪费。
  只在文字 / 字号 / 字重变化时重渲。

### 8.6 实现状态（v2.1.0 已落地 ✅）

**C-70 `SpatialGlassText`** —— `lib/presentation/widgets/kernel/spatial_glass_text.dart`
按 §8.5 三步实现，走的是**模糊版**（§8.4 的第一步）：

| 项 | 实际做法 |
|:---|:---|
| 形状图 | `TextPainter` 画进 `ui.Image`，**强制不透明白色**（遮罩只读 alpha，带 alpha 的颜色会让笔画出现"半透明的洞"） |
| 裁切 | `ShaderMask(blendMode: BlendMode.dstIn)` + `ImageShader(shape, decal, decal, m.storage)` |
| 模糊 | `BackdropFilter(ImageFilter.blur(sigmaX/Y: 2 + 12 × 强度))` |
| 清晰度 | 形状图按 `devicePixelRatio` 绘制，shader 用 `1/dpr` 缩回逻辑像素 —— 否则高 DPI 屏上字形会糊 |
| 缓存 | 只在 文字/字号/字重/字距/行高/竖排/换行宽/dpr 变化时重渲；渲染期间的新变更用 dirty 标志补做，**不丢帧** |
| 首帧 | 形状图未就绪时返回**等尺寸空盒** —— 一帧，肉眼不可见，但组件不会跳一下 |

**接线方式**：没有让 C-68 去分支，而是**把玻璃当成一个图层插进去**：

```
C-68 layers（从下到上）:  发光 → 描边 → 【玻璃 ← 新增】 → 填充
                                                └─ 填充同步淡出为 (1 − 内容玻璃)
```

⇒ 竖排、自动换行、胶囊底、渐变这些排版能力**全部自动继承**；
C-70 只管"材质"，C-68 只管"排版与装饰"，两边各自演进、互不制造 diff。

**★ 为什么填充必须淡出**：玻璃层占的正是"字芯"那一层。若实心填充照旧画上去，
会把玻璃整片盖住 —— 那根滑条看起来就**完全没反应**（会变成"改了但没生效"）。
所以 `contentGlass` 从 0 拉到 1 的过程 =「实心字 → 玻璃字」的连续过渡。

**UI**：组件样式面板新增「**内容玻璃**」滑条（0~100%，紧跟在「字号」之后），
并新增「**玻璃字**」预设（预设从 5 套变 6 套）。
★ 命名刻意与「组件外壳」区分：那是给组件垫玻璃底（卡片感），这是字本身变玻璃。
预设同理 —— `glass`（胶囊底）与新增的 `glassText`（内容玻璃）是两回事。

**还没做的两步**（§8.4 的后续）：

1. **折射版**：用 `ImageFilter.shader` + `lens_refraction.frag` 换掉 blur。
   这一步只动 C-70 内部，对外接口不变，属于"换个 filter"。
2. **导出一致性实测**：`RepaintBoundary.toImage` 的离屏路径下 `BackdropFilter`
   行为需真机验证（§8.3 第 2 条）。若与屏幕观感不一致，
   导出时降级为"预烘焙的折射贴图"。

---

## 九、组件插入分层 / 遮挡（期 6）

### 9.1 目标（用户原话）

> "自体在主体后面还是没实现（手机厂商的时钟景深）"
> —— 时钟要能**塞进景深里、被前景挡住**，而不是永远浮在最上层。

### 9.2 为什么现在做不到

`LayeredParallaxView` 在**一次 shader 调用里把所有层合成完**，输出一张图。
而组件层是堆在它**上面**的 `Stack` 兄弟节点 ⇒ **没有任何"层与层之间"的插入点**。
（把组件画在它下面也不行：最远层 alpha 恒为 1、铺满全屏，会把组件盖掉。）

### 9.3 实现路径：**两段式合成**

```
Pass 1  深度 < 组件 depth 的层      → 远景（含铺底层）
Pass 2  组件                        → 插进景深里
Pass 3  深度 > 组件 depth 的层      → 近景（主体 / 前景树枝）盖住组件
```

改动集中在两处：

| 改动 | 说明 |
|:---|:---|
| `LayeredParallaxView` 支持**只渲染层的一个子集** | 加 `fromIndex` / `toIndex`（或直接接收一个 `DepthLayerSet` 子集）。shader 不用改 —— 它本来就是按 `uCount` 逐层合成 |
| P-24 的 `Stack` 改成"按 depth 分组交错" | 组件的 `depth` 已经存在（模型注释写明"决定前后关系（遮挡）"），**不需要改模型** |

### 9.4 两个要注意的点

| # | 问题 | 对策 |
|:---|:---|:---|
| 1 | **性能**：拆成两段 = 两次 shader 调用 + 一次额外合成 | 只在**确实有组件位于主体之后**时才拆；没有就保持现在的单段渲染（零回归） |
| 2 | **边缘硬切** | 分段处是层的 alpha 边界，本来就有 feather，不会出现硬边；但要实测确认 |

### 9.5 排序键

排序直接用 `SpatialComponent.depth` 与各层的 `centerDepth` 比 —— 两者同一量纲（0 = 最远，1 = 最近），
所以"组件插在第几层之间"这件事**在模型层面已经表达完整**，期 6 只是把它落到渲染上。



