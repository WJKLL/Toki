# PLAN · 壁纸功能模块 v1.52

> **光感壁纸 P-23 + 空间壁纸 P-24 + 组合创作中心 P-25**
> 目标版本：`v1.52.0`（当前 `1.51.5+166`）
> 来源：外部对话归纳的《百工箱 · 壁纸功能模块规格书》（2026-09-11）
> 本文档 = **规格整理版 + 与本项目现状的校准 + 待决策项**
>
> **状态：方案已确认；量化链路已完成（见 §6.6），应用层尚未落地代码。**
> **编码：UTF-8 无 BOM**（见 `D:\Projects\SYNC_RULES.md` §2.3）
>
> **产品决策记录（2026-09-11）**
> - **B1 = App 内编辑导出** —— 不做系统动态壁纸（无 `WallpaperService`）
> - **A5 = 暂时放弃视频** —— 光感只做图片；视频档位与相关依赖全部暂缓
> - **B4 = 预设景深模板 + 手动修正** —— 降级链末两环连通

---

## 0. 阅读须知（三个前置结论）

### 0.1 ⚠️ 命名区分：本文的"光感"≠ 项目既有的"沉浸光感"

项目里存在**两个含义完全不同的"光感"**，极易混淆：

| 名称 | 指代 | 载体 | 文档 |
|:--|:--|:--|:--|
| **沉浸光感 / HDS** | 鸿蒙 `@kit.UIDesignKit` 的**系统材质**（玻璃模糊 / 光池 / 扫光） | App 自身 UI 组件（底栏 / 顶栏 / FAB） | `RESEARCH_immersive_glow_hds.md` |
| **光感壁纸**（本文 P-23） | 给**用户导入的图片 / 视频**叠加玻璃质感光影水印 | 用户创作产物（导出 PNG / MP4） | 本文档 |

**两者无技术继承关系，不要互相套用参数。** 仅"自绘近似"的实现思路可参考。

### 0.2 本文档的标注约定

| 标记 | 含义 |
|:--|:--|
| ✅ | 已用实测/源码核实，与项目现状一致 |
| ⚠️ | **与事实冲突**，须解决后才能开工（见 §9-A） |
| ❓ | 需产品决策（见 §9-B） |
| 💡 | 整理时补入的信息（原规格书没有） |

### 0.3 本次整理做了什么

- 去重：模型信息原分散在 §3.4 / §6.1 / §6.2，已合并至 §6
- 补链：补齐 §3.7 输入源抽象 → §3.3 / §3.6 的依赖关系（原文未点明）
- 校准：用实测数据核对全部技术前提，标出 7 处冲突（**2026-09-11 已消解 3 处**，见 §9-A）
- 重建：原规格书 §10 / §11 复制损坏（乱码），见 §9-B2
- 登记：编号与 `CODE_REFERENCE.md` 对齐（见 §8）

---

## 1. 总览

```
   光感壁纸 P-23              空间壁纸 P-24
  （无 AI，纯 GPU）    ◄────  （AI 深度 + 视差）
                    组合 P-25
```

| 功能 | 本质 |
|:--|:--|
| **光感壁纸** | 给任意图片/视频加玻璃质感光影水印；可导入 Logo、自定义字体 |
| **空间壁纸** | AI 生成深度图 → 用户点选/拖动切换焦点 → 组件带 3D 立体感 → 导出图片/视频 |
| **组合** | 空间壁纸导出物作为素材进入光感编辑器，与相册图片同等处理 |

### 1.1 依赖链（💡 原文未点明，此处补全）

```
§3.7 输入源抽象 (SpatialInputSource)
   ├── SensorInputSource      → §3.3.3 陀螺仪驱动
   ├── JoystickInputSource    → Web / 无障碍
   └── RecordingInputSource   → §3.6 导出（程序化轨迹）
        ↓
   渲染层（FragmentShader 逐像素位移）
        ↓
   焦点深度 (§3.3.1) + 组件 6 自由度 (§3.2 / §3.3.2)
        ↓
   导出 (S-32) → 组合 (P-25) → 光感编辑器 (P-23)
```

**关键设计含义**：**深度推理只跑一次**（不是每帧）。拖拽焦点、陀螺仪晃动时，实时的只有 shader 位移。
这是 §2.3「预览 ≥ 55fps」能够成立的前提，也决定了深度后处理（§3.5）只需在推理后执行一次。

---

## 2. 光感壁纸 · P-23

### 2.1 可替换内容

| 项 | 格式 | 说明 |
|:--|:--|:--|
| 背景图 | JPG / PNG | 相册导入 / 内置模板 |
| Logo / 图案 | PNG / SVG | 透明 PNG 最佳；SVG 矢量不糊 |
| 字体 | .ttf / .otf / .woff2 | 用户可导入（见 §5.1） |
| 文字内容 | 任意文本 | 支持换行、多段文字 |
| 视频 | MP4 | 逐帧加光感（性能敏感，见 2.4） |

### 2.2 光感参数（6 项可调）

| 参数 | 范围 | 默认 |
|:--|:--|:--|
| 强度 | 0.0 ~ 1.0 | 0.6 |
| 方向 | 0° ~ 360° | 135° |
| 色温 | 冷白 ~ 暖白 | 中性 |
| 透明度 | 0.0 ~ 1.0 | 0.8 |
| 模糊度 | 0 ~ 20 px | 8 px |
| 阴影强度 | 0.0 ~ 1.0 | 0.4 |

### 2.3 技术实现

规格书原写：`ShaderMask` + `LinearGradient` + `BlendMode.softLight` 模拟玻璃光感，**不走自定义 shader**（跨平台兼容性最佳）。

- ✅ **"不走自定义 shader"的方向与本项目一致**：Web 端 FragmentShader 支持情况不确定，`u03_blur_policy.dart` 已有「Web 关」的既有结论。
- ✅ **且因 A5 放弃视频，原 §9-A5 的冲突（`ShaderMask` 属 Widget 层、无法逐帧）自动消解** —— 纯图片路径下 `ShaderMask` 完全可用，`S-30` 无需改走 Canvas 路线。

### 2.4 视频光感合成 —— ⏸ 暂缓（决策 A5）

> **2026-09-11 决策：暂时放弃视频。**
> 本节、§2.5 的视频档、§3.6 视频导出，以及 `S-33`（视频光感合成服务）、`U-09`（轨迹模板库）、`RecordingInputSource`、`ffmpeg_kit_minimal` 依赖 **一并暂缓**。
>
> **连带收益**：原 §9-A3（鸿蒙 FFmpeg 不可用）与 §9-A5（Widget 无法逐帧）**两条硬冲突自动消解**；`S-30` 光感合成服务**不必**为视频改走 Canvas 路线。

- 逐帧应用光感
- **边解码 → 边合成 → 边编码**，防 OOM
- 提供性能提示 UI（预计耗时 + 降级建议）
- 可取消

### 2.5 导出

| 类型 | 格式 | 分辨率档 | 状态 |
|:--|:--|:--|:--|
| 图片 | PNG / JPG | 720p / 1080p / 原图 | ✅ 本期 |
| 视频 | MP4 | 720p / 1080p | ⏸ 暂缓（A5） |

### 2.6 💡 可复用的现有能力

| 现有资产 | 位置 | 用途 |
|:--|:--|:--|
| 图片保存 | `core/media/image_saver_service.dart`（v1.40.0） | 导出直接复用（Android → MediaStore，Web → 浏览器下载） |
| 视觉参数集中模式 | `c22_visual_params.dart`（v1.7.6） | 6 个光感参数的单一事实源范式（对齐 `U-10`） |
| 模糊策略 | `core/utils/u03_blur_policy.dart` | σ ≤ 20 的既有红线；本功能"模糊度 0~20px"与之天然一致 ✅ |

---

## 3. 空间壁纸 · P-24

### 3.1 可替换内容

| 项 | 格式 | 说明 |
|:--|:--|:--|
| 背景图 | JPG / PNG | 相册导入 |
| 组件 | 内置 / 自定义 | 时钟 / Logo / 文字贴纸 / 自定义图片 |
| Logo | PNG / SVG | 同光感壁纸 |
| 字体 | .ttf / .otf / .woff2 | 同光感壁纸 |

### 3.2 组件类型与自由度

| 组件 | 说明 |
|:--|:--|
| 数字时钟 | 可换字体、颜色、大小 |
| 模拟时钟 | 指针样式、刻度样式 |
| Logo 贴纸 | 用户导入 PNG / SVG |
| 文字贴纸 | 任意文本 + 自定义字体 |
| 图片贴纸 | 用户导入任意图片 |

**每个组件独立拥有 6 个自由度**：

```
位置 (X / Y) + 深度 (Z) + 朝向 (tiltX / tiltY) + 缩放 (scale)
```

### 3.3 核心交互

#### 3.3.1 切换焦点深度

| 手势 | 效果 |
|:--|:--|
| 单击画面 | 该点成为焦点，瞬时 |
| 拖拽画面 | 焦点连续跟随手指移动，实时预览 |
| 双指上下滑 | **只调焦点深度**，不改位置 |

> 用户看到的效果：焦点处"钉住"不动，前景和背景反向移动，产生强烈立体感。

#### 3.3.2 组件贴纸操作

| 手势 | 行为 |
|:--|:--|
| 单指拖拽组件 | 移动位置（X / Y） |
| 双指拖拽组件 | 水平移动位置 + 垂直调整深度（Z） |
| 双指旋转 | 调整朝向（tiltX / tiltY） |
| 双指捏合 | 调整大小（scale） |
| 长按拖出画布 | 删除组件 |

#### 3.3.3 深度联动视觉反馈

- 组件越**近**（Z 大）→ 阴影越大越深、透视角度越小
- 组件越**远**（Z 小）→ 阴影越小越淡、透视角度越大
- **陀螺仪驱动时，组件自身也跟着 3D 旋转**（不只位移）
- 多个组件深度不同 → 晃动时移动幅度不同，形成真立体层次

> ⚠️ 3.3.1 与 3.3.2 的手势语义重叠（见 §9-A6）。

### 3.4 AI 深度估计

| 项 | 内容 |
|:--|:--|
| 模型 | YOLO26 depth —— ⚠️ 原文写"n / s / m 三档"，与 §6.1「m 暂不部署」矛盾；**以「部署 n/s，m 预留」为准** |
| 格式 | **FP32 ONNX（基线）+ FP16 ONNX（主力）**，✅ 不用 INT8 |
| 推理引擎 | ONNX Runtime（各平台注册不同实现） |
| 设备分级 | `device_capability` 插件 → 四档兜底 |
| 失败兜底 | 预设景深模板（不依赖 AI）❓ 具体形态待定，见 §9-B4 |

**设备分级策略**：

| 档位 | 模型 | 输入分辨率 |
|:--|:--|:--|
| low | yolo26n FP16 | 384×384 |
| medium | yolo26n FP16 | 512×512 |
| high | yolo26s FP16 | 640×640 |
| ultra | yolo26s FP16 | 768×768 |

> ✅ **2026-09-11 已验证（原 §9-A1 已解决）**：`dynamic=True` 导出的 **FP16** 模型在 384 / 512 / 640 / 768 四档**全部跑通**，输出 shape 严格跟随输入（`(1,1,N,N)`）。
> **本表由 2 个文件覆盖**（`yolo26n-depth_dynamic_fp16.onnx` + `yolo26s-depth_dynamic_fp16.onnx`），无需 4 个固定尺寸模型。

### 3.5 深度图质量把控

| 环节 | 内容 |
|:--|:--|
| 质量评估 | 5 维度评分：动态范围 / 边缘锐度 / 分离度 / 伪影 / 一致性 |
| 后处理 | 归一化 → 双边滤波 → 引导滤波 → 边缘强化 |
| 可视化 | 深度热力图（开关切换） |
| 诊断面板 | 显示评分 + 建议 |
| 降级链 | 换模型 → ROI 重推理 → 预设景深 → 手动分层 → 拒绝 |

> 💡 后处理只需在推理后执行一次（见 §1.1），不必进入每帧热路径。

### 3.6 导出

| 类型 | 格式 | 分辨率档 | 状态 |
|:--|:--|:--|:--|
| 图片 | PNG / JPG | 720p / 1080p / 原图 | ✅ 本期（`S-32`） |
| 视频 | MP4 | 720p / 1080p | ⏸ 暂缓（A5） |

**视频导出的核心设计 —— ⏸ 暂缓（决策 A5）**：

- 导出时把输入源切换为 `RecordingInputSource`（程序化注入轨迹）
- 轨迹模板：正弦晃动 / 圆形环绕 / 推拉 / 用户录制
- 边生成边编码，防 OOM
- ~~移动端 `ffmpeg_kit_minimal`；Web 用 `MediaRecorder`~~ ← 随视频一并暂缓

> 💡 这套设计**解决了"传感器视差"与"导出固定视频"的天然矛盾**：导出时不用真实传感器，而是程序化注入等效轨迹。
> **若将来重启视频，本节设计可直接复用。**

### 3.7 输入源抽象（关键设计）

| 实现 | 使用场景 |
|:--|:--|
| `SensorInputSource` | 移动端（陀螺仪） |
| `JoystickInputSource` | Web 端（虚拟摇杆）/ 无障碍用户 |
| `RecordingInputSource` | 视频导出（程序化注入轨迹） |

**渲染层只认 `SpatialInputSource` 接口，三种场景复用同一渲染管线。**

### 3.8 💡 可复用的现有能力

| 现有资产 | 位置 | 用途 |
|:--|:--|:--|
| **FragmentShader 基础设施** | `pubspec.yaml` 的 `shaders:` 段 + `kernel/lens.dart` | ✅ **视差位移有现成范式**：`lens_refraction.frag` / `bloom_dual_peak.frag` 已在使用，`LensRefraction` 已有 `depthEffect` uniform 与 `setFloat` 传参范例 |
| 预模糊缓存 | `kernel/blur.dart` `BackdropBlur`（+ `c27_prefrosted_blur.dart`） | 若需背景模糊，走缓存路径（既有 P0 结论） |
| 阻尼拖拽 | `kernel/damped_drag.dart` | 焦点拖拽的橡皮筋手感参考 |
| 视觉参数单一事实源 | `c22_visual_params.dart` | 对齐 `U-10` |

---

## 4. 组合使用 · P-25

**定义**：空间壁纸的导出物（图片 / 视频）作为素材进入光感壁纸编辑器，与相册图片同等处理。

| 场景 | 流程 |
|:--|:--|
| 图片 → 光感 | 空间壁纸导出 PNG → 光感编辑器加水印 → 导出 |
| 视频 → 光感 | 空间壁纸导出 MP4 → 光感编辑器逐帧加水印 → 导出 |
| 串联工作流 | P-25 聚合入口一次完成：选图 → 空间处理 → 光感 → 导出 |

**技术预留**：

- 素材来源抽象（`U-11`）：`AlbumInputSource` / `InternalInputSource` / `ClipboardInputSource`
- 光感合成能力下沉到服务层（`S-30`），空间壁纸组件可直接调用

---

## 5. 共享能力

### 5.1 字体管理（U-14）

| 功能 | 说明 |
|:--|:--|
| 导入字体 | 文件选择器选 .ttf / .otf / .woff2 |
| 存储 | 应用私有目录 `fonts/`，**不打进 APK** |
| 列表管理 | 命名、删除、预览 |
| 使用范围 | 光感壁纸 + 空间壁纸共用 |
| 预览 | 选之前可先看效果 |

### 5.2 Logo / 图案管理（C-58）

| 功能 | 说明 |
|:--|:--|
| 导入 | PNG（透明）/ SVG |
| 后处理 | 自动去白底（可选）、自动裁透明边、抗锯齿 |
| 使用 | 光感水印图案 / 空间壁纸组件 / 时钟嵌入 |

### 5.3 图片导出

复用 v1.40.0 `core/media/image_saver_service.dart`：

- Android：MediaStore 保存到相册
- Web：浏览器下载
- 支持 720p / 1080p / 原图

### 5.4 视频导出 —— ⏸ 暂缓（决策 A5）

> **暂时放弃视频**，本节整节搁置（原 §9-A3 的鸿蒙 FFmpeg 冲突随之消解）。若将来重启视频，需先重新选型。

| 平台 | 原方案 | 体积 |
|:--|:--|:--|
| Android | `ffmpeg_kit_minimal` | +8~12 MB |
| iOS | `ffmpeg_kit_minimal` | +10~15 MB |
| Web | `MediaRecorder` | 0 |
| HarmonyOS | ⚠️ 原文写"复用 Android ffmpeg"（**不成立**，需独立 OHOS 构建） | +8~12 MB |

---

## 6. AI 模型规格

### 6.1 模型清单

| 模型 | 用途 |
|:--|:--|
| `yolo26n-depth` | 低 / 中端机 |
| `yolo26s-depth` | 高端机 |
| `yolo26m-depth` | 可选，最高精度（**暂不部署**） |

### 6.2 格式路线（已决策）

```
.pt（PyTorch 原始 —— 实为 FP16 权重）
    ↓
FP32 ONNX（基线，用于质量对照）
    ↓
FP16 ONNX（部署主力）
```

**放弃 INT8 的理由**：深度估计是**回归任务**，INT8 全整型输出仅 256 级，会导致深度图台阶伪影，直接毁掉视差效果。

> 💡 **整理时补入的官方实证**：ultralytics 源码 `engine/exporter.py` L1727-1729 注释记录了官方在 Hailo-8L 上的实测结论 ——
> *"the single dense depth logit both need the wider activation to keep their range (**a8 collapses the depth map; validated on Hailo-8L**)"*
> 即 **8bit 激活会让深度图坍缩**。这为"放弃 INT8"提供了官方依据，不只是理论推断。

### 6.3 💡 模型实测数据（本次核实，供体积/精度预算参考）

| 模型 | 参数量 | `.pt` 实际大小 | FP32 ONNX 预估 | FP16 ONNX 预估 |
|:--|--:|--:|--:|--:|
| yolo26n-depth | 6,355,617 | 12.4 MB | ≈ 24 MB | **≈ 12 MB** |
| yolo26s-depth | 13,223,681 | 25.5 MB | ≈ 50 MB | ≈ 25 MB |
| yolo26m-depth | 23,280,257 | 44.8 MB | ≈ 89 MB | ≈ 44 MB |

**实测结论**：

- 参数量 ×2 与 `.pt` 文件大小**精确吻合** → 说明**官方 `.pt` 权重本就是 FP16**
- 因此 **FP16 转换 = 保持原生精度 + 体积减半**，不是"降精度"
- **nano FP16 仅约 12 MB** → 打进 App 完全可接受

**输出规格（实测，全零输入验证）**：

| 项 | 值 |
|:--|:--|
| 输入 | `[1, 3, 640, 640]` float32，**归一化到 [0,1]** |
| 输出 | **`[1, 1, 640, 640]` float32 单通道深度图，单位米** |
| NaN / Inf | 无 |
| 标定 | `cal_a = 1.0`（三档一致），`cal_b` = -0.194 / +1.715 / +1.631 → **官方已做 scale-only 标定**，输出为**米制绝对深度** |

### 6.4 端侧部署形态（⚠️ 见 §9-A2 / A4）

| 平台 | 格式 |
|:--|:--|
| Android | FP16 ONNX（或从 ONNX 转 TFLite FP16） |
| Web | FP16 ONNX |
| HarmonyOS | ⚠️ FP16 ONNX（或转 TFLite FP16） |
| iOS | ⚠️ FP16 ONNX（或转 CoreML FP16） |

> 💡 **补一条原文没有的好消息**：ultralytics 8.4.146 **自带 `nn/backends/litert.py`**（LiteRT = TFLite 后继者）。
> 因此"转 TFLite"**不需要 `onnx2tf`** —— 此前环境报告里"TFLite 要 3 跳、报错难查"的论据**不成立**。

> ⚠️ 另需注意：**视差只关心相对层次与边缘质量**，不关心绝对米数。§6.3 的绝对深度标定对"空间壁纸"本身不构成必需精度要求，评估指标应以**层次分离度 / 边缘锐度 / 台阶伪影**为主。

### 6.5 当前进度

| 阶段 | 状态 |
|:--|:--|
| 环境搭建 | ✅ 完成（Python 3.11.9 + torch 2.11.0+cu128 + onnx 1.17.0 + ultralytics 8.4.146，CUDA 可用，sm_120） |
| 资源下载 | ✅ 完成（3.78 GB：3 个 `.pt` + val2017.zip + wheels） |
| 模型可加载 | ✅ 完成（`task=depth`） |
| 导出 FP32 ONNX | ✅ **完成**（2026-09-11，static + dynamic 各 2 个） |
| 导出 FP16 ONNX | ✅ **完成**（体积 −49.6% / −49.8%） |
| 精度评估 | ✅ **完成**（abs_rel 0.05% / 0.02%，δ1 = 1.00000，无可见台阶）—— 见 §6.6 |
| Flutter 端接入 | ⏳ 未开始 |

**工作目录**：`D:\Projects\mode`（量化工程，独立于本仓库）
**产出目录**：`D:\Projects\mode\onnx\`（6 个 `.onnx`）
**脚本目录**：`D:\Projects\mode\yolo_work\`

### 6.6 量化实测结果（2026-09-11）

**产出清单**（`D:\Projects\mode\onnx\`）：

| 文件 | 体积 |
|:--|--:|
| `yolo26n-depth_fp32.onnx` | 19.8 MB |
| `yolo26n-depth_dynamic.onnx` | 19.8 MB |
| **`yolo26n-depth_dynamic_fp16.onnx`** | **10.0 MB** |
| `yolo26s-depth_fp32.onnx` | 45.9 MB |
| `yolo26s-depth_dynamic.onnx` | 46.0 MB |
| **`yolo26s-depth_dynamic_fp16.onnx`** | **23.0 MB** |

**IO 规格**（实测）：

| 项 | 值 |
|:--|:--|
| 输入 | `['batch', 3, 'height', 'width']` float32，**归一化到 [0,1]**（letterbox + `/255`，ultralytics 约定） |
| 输出 | `['batch', 1, 'height', 'width']` float32，**单通道深度图，单位米** |
| 四档实测 | 384 / 512 / 640 / 768 全部跑通，输出 shape 严格跟随输入 |
| dtype | `tensor(float)`（`keep_io_types=True`）→ **端侧无需准备 FP16 缓冲区** |

**精度：FP16 vs FP32**（12 张真实图 @640 letterbox，val2017 流式读取，未解压）：

| 模型 | abs_rel | δ1 | RMSE | max abs err |
|:--|--:|--:|--:|--:|
| nano | **5.008e-04**（0.05%） | **1.00000** | 2.171e-03 m | 8.701e-03 m |
| small | **1.960e-04**（0.02%） | **1.00000** | 7.693e-04 m | 2.902e-03 m |

相对误差分布：p50 ≈ 2e-4，p99 ≈ 1e-3，max ≈ 2.7e-3 —— 与 FP16 理论精度（2⁻¹¹ ≈ 4.9e-4）**吻合**，说明误差即浮点舍入本身，**未在 88 层卷积中累积放大**。

**台阶伪影检查**：

| 平坦区定义 | FP32 梯度 | FP16 梯度 | 比值 |
|:--|--:|--:|--:|
| `< 0.01 mm` | 2.4e-06 | 3.5e-06 | 1.20 – 1.57 |
| `< 0.1 mm` | 4.6e-05 | 4.7e-05 | **1.006 – 1.011** |

极平坦区 FP16 梯度被放大 1.2–1.57×，但**绝对幅度仅 3.5 微米**，远低于任何可视阈值 → **无可见台阶**。

**CPU 延迟**（x86 本机，`onnxruntime 1.29.0` CPU EP；**非手机数据**）：

| 用例 | 模型加载 | 单帧推理 |
|:--|--:|--:|
| nano FP32 | 0.12 s | 70.0 ms |
| nano FP16 | 0.20 s | 73.1 ms（**+4.4%**） |
| small FP32 | 0.17 s | 101.1 ms |
| small FP16 | 0.24 s | 112.5 ms（**+11.3%**） |

> ⚠️ x86 数据**不能外推**到 ARM 手机（ARMv8.2-A 有原生 NEON FP16）。**端侧实测仍是必需项。**

**验收标准对照**（§2.2 目标）：

| 目标 | 阈值 | 实测 | 结论 |
|:--|:--|:--|:--|
| 模型体积 | 相比 FP32 −50% | −49.6% / −49.8% | ✅ |
| abs_rel 差异 | < 3% | **0.05% / 0.02%** | ✅ 好 60–150 倍 |
| δ1 差异 | < 1% | **0.00000%** | ✅ 完全一致 |
| 无视觉伪影 | 无台阶 / 条纹 | 最坏 3.5 微米 | ✅ |
| CPU 耗时 | 不劣化 > 20% | +4.4% / +11.3%（x86） | ✅ 本机达标 |

**三个附带发现**：

1. **FP16 图仅多 7 个 Cast**（425 vs 418 节点；88 个 Conv 全部在 FP16）→ ORT 的 FP16 转换是**整体性**的，不是逐算子 Cast。
2. **CPU EP 缺 FP16 的 `Exp` kernel** —— 运行时报 `Could not find a CPU kernel ... can't constant fold Exp node '/model.23/Exp_1'`。该节点正是深度 head 尾部的 `exp(clamp(...))`。**正确性不受影响**，但印证了深度模型尾部对精度的高要求（与放弃 INT8 是同一处），也是 CPU 上 FP16 略慢的成因。
3. **`.pt` 权重本就是 FP16**（参数量 ×2 与文件大小精确吻合）→ FP16 转换是"**保持原生精度 + 体积减半**"，不是降精度。

**相关脚本**（`D:\Projects\mode\yolo_work\`）：`export_onnx.py`、`verify_onnx.py`、`convert_fp16.py`、`compare_onnx.py`、`verify_fp16.py`、`bench_cpu.py`

---

## 7. 技术栈与平台矩阵

### 7.1 新增依赖

| 依赖 | 用途 | 引入阶段 | 备注 |
|:--|:--|:--|:--|
| `flutter_widget_watermark` | 光感合成 | 光感壁纸 | ⚠️ 当前仅 **0.1.0**，成熟度存疑 |
| `onnxruntime` | 深度推理 | 空间壁纸 | 需确认对 OH / 各平台的支持 |
| `device_capability` | 设备分级 | 空间壁纸 | ⚠️ 当前仅 **0.1.2** |
| `sensors_plus` | 陀螺仪 | 空间壁纸 | |
| ~~`ffmpeg_kit_minimal`~~ | 视频编码 | ⏸ **暂缓（A5）** | 原 ⚠️ 上游已退休；随视频一并暂缓，§9-A3 自动消解 |

### 7.2 复用现有

| 复用项 | 来源 | 用途 |
|:--|:--|:--|
| `image_saver_service.dart` | v1.40.0 | 图片保存 |
| `log_export_service.dart` | S-13 | 错误处理参考 |
| `c22_visual_params.dart` | v1.7.6 | 视觉参数集中模式 |
| P-05 编辑交互 | v1.19.x | BottomSheet 参考 |
| P-20 / P-21 管理页模式 | v1.50.0 | 编辑器结构参考 |

### 7.3 项目约束（✅ 已与 pubspec 核对）

| 约束 | 现状 |
|:--|:--|
| UI：`flutter_miuix`，禁 Material 视觉组件 | ✅ `flutter_miuix ^1.1.1` |
| 状态：Riverpod | ✅ `flutter_riverpod ^3.4.2` |
| 路由：go_router | ✅ `go_router ^18.0.0` |
| 渲染后端：Skia（`EnableImpeller=false`） | ✅ README 一致 |
| 存储：SharedPreferences | ✅ `^2.5.5` |
| 版本规则：功能 → Minor，修复 → Patch，只升不降 | ✅ |
| 编号体系：P / C / S / U / R / A / F | ✅ |
| 视觉参数集中到单一事实源 | ✅ 参照 `c22_visual_params.dart` |

### 7.4 平台支持矩阵

| 能力 | Android | Web | HarmonyOS | iOS |
|:--|:--|:--|:--|:--|
| 光感壁纸 | ✅ | ✅ | ✅ | ✅ |
| 空间壁纸 | ✅ | ✅ | ✅ | ✅ |
| 深度推理 | ONNX RT | ONNX RT Web | ⚠️ **待定**（原 ✅） | ⚠️ **待定**（原 ✅） |
| 视差渲染 | FragmentShader | FragmentShader | FragmentShader | FragmentShader |
| 视频导出 | ⏸ 暂缓（A5） | ⏸ 暂缓（A5） | ⏸ 暂缓（A5） | ⏸ 暂缓（A5） |
| 陀螺仪 | ✅ | 摇杆替代 | ✅ | ✅ |
| 字体导入 | ✅ | ✅ | ✅ | ✅ |
| Logo 导入 | ✅ | ✅ | ✅ | ✅ |

> 💡 **"HarmonyOS" 实际指 OpenHarmony**：据 `RESEARCH_immersive_glow_hds.md` §7.1，实际验收设备为 **OpenHarmony 6.1.1.120 / API 24 / arm64**（非华为 HarmonyOS）。这对 §9-A2 的推理运行时选型有决定性影响。
>
> ⏸ **视频导出全平台暂缓（决策 A5）**：原方案 Android / HarmonyOS 用 `ffmpeg_minimal`、Web 用 `MediaRecorder`、iOS 用 `ffmpeg_minimal`，随"暂时放弃视频"一并搁置；**§9-A3 的鸿蒙 FFmpeg 冲突因此自动消解**。

---

## 8. 编号登记

> 已与 `CODE_REFERENCE.md` 核对，无冲突。现有最大编号：S-26 / C-52 / P-20 / U-05 / R-16 / A-05 / F-10。

### 8.1 光感壁纸 P-23

| 编号 | 名称 |
|:--|:--|
| P-23 | 光感壁纸编辑器 |
| C-57 | 光感参数调节卡 |
| C-58 | Logo 导入选择器 |
| S-30 | 光感合成服务 |
| R-19 | `/glow-wallpaper` |

### 8.2 空间壁纸 P-24

| 编号 | 名称 |
|:--|:--|
| P-24 | 空间壁纸编辑器 |
| C-59 | 虚拟摇杆 |
| C-60 | 导出配置弹层 |
| C-61 | 深度图可视化组件 |
| C-62 | 效果诊断面板 |
| C-64 | 焦点控制器（拖拽 / 双指） |
| C-65 | 组件贴纸控制器 |
| S-31 | 深度推理服务 |
| S-32 | 空间壁纸导出服务 |
| S-34 | 深度质量评估服务 |
| U-08 | 输入源适配器 |
| U-09 | 轨迹模板库 ⏸ 暂缓（A5） |
| U-12 | 深度后处理管线 |
| A-08 | 焦点切换交互 |
| A-09 | 组件拖动交互 |
| R-20 | `/spatial-wallpaper` |

### 8.3 组合使用 P-25

| 编号 | 名称 |
|:--|:--|
| P-25 | 壁纸创作中心（聚合入口） |
| R-21 | `/wallpaper-studio` |
| S-33 | 视频光感合成服务 ⏸ 暂缓（A5） |
| U-10 | 视觉参数单一事实源（共用） |
| U-11 | 素材来源适配器（共用） |

### 8.4 共享能力

| 编号 | 名称 |
|:--|:--|
| U-14 | 字体管理工具 |
| C-63 | 字体导入选择器 |
| F-11 | 壁纸创作功能模块（统领）✅ 已核实可用 |

### 8.5 模型相关

| 编号 | 名称 |
|:--|:--|
| S-35 | 内置模型管理服务 |
| S-36 | 用户偏好记忆服务 |
| P-26 | 模型信息页 |
| R-22 | `/model-info` |

> ❓ 编号缺口：现有 `S-27~29` / `C-53~56` / `P-21~22` / `U-06~07` / `R-17~18` / `A-06~07` 均跳过未用（见 §9-B3）。

---

## 9. 待决策项

### A 类 · 技术硬冲突

> **2026-09-11 状态：7 条中已消解 3 条**（A1 已解决；A3 / A5 因决策 A5 放弃视频而自动消解），**剩余 4 条**（A2 鸿蒙 / A4 iOS / A6 手势 / A7 措辞）。

#### ~~A1 ⭐ 四档分辨率 vs ONNX 固定输入尺寸~~ ✅ 已解决

原问题：§3.4 要求 384 / 512 / 640 / 768 四种输入，而 `dynamic=False` 只支持固定 640。

**解决方式**：采用方案 (a) —— 导出 `dynamic=True` 模型，端侧自行 resize。
**验证结果（2026-09-11 实测）**：**FP16 dynamic 模型在 384 / 512 / 640 / 768 四档全部跑通**，输出 shape 严格跟随输入 `(1,1,N,N)`；且 dynamic 与 static 在 640 下输出**逐位一致**（排除了动态导出的数值偏差）。

| 方案 | 代价 | 结论 |
|:--|:--|:--|
| **(a) `dynamic=True` 的 n / s 两个模型** | 端侧 resize | ✅ **已采用**，只需 2 个文件（ultralytics 对 DepthModel 有专门动态导出分支，`exporter.py` L1093-1094） |
| (b) 导出 4 个固定尺寸模型 | 体积 ×2 | 未采用；**作为端侧性能不利时的后备** |
| (c) 统一 640，放弃分档分辨率 | low 档无提速 | 未采用 |

**→ 唯一遗留**：dynamic 的灵活性 vs static 的 EP 优化收益，**须由端侧真机实测裁决**（已列入 §11 阶段 1 必测项）。

#### A2 ⭐ 鸿蒙：ONNX Runtime 与 LiteRT 均无官方支持

§6.4 与 §7.4 把鸿蒙当现成能力，但两条前提都不成立：

- **ONNX Runtime 无官方 OHOS 发行版** → 需自行交叉编译
- **LiteRT / TFLite 同样无 OHOS 支持**

**实际可选路径**：自编译 ORT-ohos / 转 **MindSpore Lite** / 华为 HiAI / NPU 工具链。

**→ §7.4 矩阵中该格已标为 ⚠️ 待定。**
**→ 且实际设备是 OpenHarmony 6.1.1.120 / API 24，不是华为 HarmonyOS，连华为侧工具链也可能不适用。**

#### ~~A3 `ffmpeg_kit_minimal` 不能"复用 Android"~~ ⏸ 已消解（A5 放弃视频）

原问题：§5.4 —— Dart 包原生库**按平台绑定**，鸿蒙需独立的 OHOS 原生构建，"复用 Android ffmpeg"不成立。且 **FFmpegKit 上游已停止维护**，现由[社区分支](https://github.com/ffmpegkit-maintained/ffmpeg)接续（Android 导向，无鸿蒙支持）。

**消解方式**：决策 A5「暂时放弃视频」→ `ffmpeg_kit_minimal` 不再引入。

**→ ⚠️ 若将来重启视频，本条会立即复活**，届时需重新选型或自建 OHOS FFmpeg 构建。

#### A4 iOS CoreML 在当前环境做不了

`coremltools` **仅支持 macOS**。当前是 Windows 环境，**无法产出 CoreML**。
iOS 那条路需要 Mac 或 CI。**→ §7.4 该格已标为 ⚠️ 待定。**

#### ~~A5 ⭐ 光感：图片走 Widget，视频走不了 Widget~~ ⏸ 已消解（决策 A5 放弃视频）

原问题：§2.3 用 `ShaderMask`（Widget 层能力），§2.4 又要求"逐帧应用光感"——**视频帧没有 Widget 树，`ShaderMask` 无法逐帧使用**。

**消解方式**：决策 A5「暂时放弃视频」→ 纯图片路径下 `ShaderMask` 完全可用，`S-30` **不必**改走 Canvas 路线。

**→ 遗留小项**：`flutter_widget_watermark` 仅 **0.1.0**，能否满足 6 个参数（强度 / 方向 / 色温 / 透明度 / 模糊度 / 阴影）需在阶段 4 实测；若不满足，自行用 `ShaderMask` + `ImageFilter.blur` 实现。

#### A6 手势语义重叠，缺命中优先级

§3.3.1（焦点）与 §3.3.2（组件）**都占用单指拖拽 + 双指拖拽**。

**→ 需明确规则：命中组件 → 手势归组件；命中空白区 → 归画布。**

#### A7 §3.4 措辞与 §6.1 冲突

§3.4 写"n / s / m 三档"，§6.1 说 m"暂不部署"。
**→ 已按"部署 n/s，m 预留"统一（见 §3.4）。**

---

### B 类 · 需要产品确认

| # | 问题 | 影响 |
|:--|:--|:--|
| ~~**B1**~~ ✅ **已确认** | **系统动态壁纸？** → **不做**，只做 **App 内编辑 + 导出**。传感器视差仅在 App 内预览体现，不注册 `WallpaperService` | 架构保持"一个编辑器 + 导出"，无需原生壁纸服务 |
| **B2** | **原规格书 §10 / §11 复制损坏（乱码）**，无法整理。仅能辨认出 `…26-depth.onnx（Web 端）` | 交付物清单与"待定项"缺失，需重发 |
| **B3** | **编号跳号是否有意？** 跳过 `S-27~29`/`C-53~56`/`P-21~22`/`U-06~07`/`R-17~18`/`A-06~07` | 预留还是遗漏 |
| ~~**B4**~~ ✅ **已确认** | **"预设景深模板"形态？** → **方案 C+D**：提供 **2–3 套固定模板**（径向 / 线性 / 上下分带）+ **可手动修正** | 降级链末两环连通：选模板 → 手动画几笔修正，UX 顺畅 |
| **B5** | **`device_capability`（0.1.2）的 4 档判定依据？** | RAM / CPU 核数 / 有无 NPU？API 能否支撑 |
| **B6** | **顺序确认**：§1 说光感壁纸**先行**，但 §6.5 实际在做**空间壁纸**的模型 | 并行还是改序 |
| **B7** | **精度评估的指标基线**：视差关心相对层次与边缘，不关心绝对米数 | 是否采用「结构/边缘优先」的评估口径（见 §6.4 备注） |

---

## 10. 与现有调研的衔接

| 现有文档 | 关系 |
|:--|:--|
| `RESEARCH_immersive_glow_hds.md` | 研究对象是 **HDS 系统材质**（App 自身 UI 的玻璃光感），与本文 P-23 **同名不同义**（见 §0.1）。仅"自绘近似"思路可参考；其记录的设备结论（OpenHarmony 6.1.1.120 / API 24）与本文 §9-A2 直接相关 |
| `PLAN_widget_v1.51.md` | 桌面小组件 F-10 / S-26，**与本文无冲突**（编号不重叠） |
| `CODE_REFERENCE.md` | 本文 §8 编号已与其核对 |
| `CHANGELOG.md` | 落地时按 Minor 版本（v1.52.0）追加 |
| `README.md` | 落地后补「壁纸」特性行 |

---

## 11. 落地顺序

| 阶段 | 内容 | 前置 | 状态 |
|:--|:--|:--|:--|
| **阶段 0** | 导出 FP32 / FP16 ONNX + 精度验证 | — | ✅ **已完成**（见 §6.6） |
| **阶段 1** | `S-31` 深度推理服务 + `U-12` 后处理管线（端侧跑通一张图） | 阶段 0 | ⏳ **可开工** |
| **阶段 2** | `P-24` 空间壁纸编辑器：`U-08` 输入源抽象 + 视差 shader + `A-08` 焦点交互 | 阶段 1 | ⏳ |
| **阶段 3** | 组件体系（§3.2 / §3.3.2）+ `A-09` | 阶段 2 | ⏳ |
| **阶段 4** | `P-23` 光感壁纸 + `S-30` 合成服务（**仅图片路径**） | — | ⏳ |
| **阶段 5** | `P-25` 组合 + `S-32` 图片导出 | 阶段 2、4 | ⏳ |
| **阶段 6** | 平台补齐（鸿蒙 / iOS，**取决于 A2 / A4 的裁决**） | 全部 | ⏳ |

### 11.1 阶段 1 必测项（新增）

1. **端侧真机延迟**（ARM CPU / NNAPI）—— 本机 x86 数据**不可外推**（见 §6.6）
2. **dynamic vs static 裁决** —— 若 dynamic 在目标机上明显偏慢，退回方案 (b)，导出 4 个固定尺寸模型（见 §9-A1）
3. **FP16 vs FP32 裁决** —— 若目标机 CPU EP 上 FP16 无收益且更慢，可只发 FP32（体积换速度）

### 11.2 暂缓项（决策 A5）

阶段 5 的视频导出、`S-33`（视频光感合成服务）、`U-09`（轨迹模板库）、`RecordingInputSource`、`ffmpeg_kit_minimal` 依赖 —— **全部搁置**。

---

*整理：2026-09-11 · 基于外部对话规格书 + 本项目源码/环境实测校准*
*更新：2026-09-11 · 落入产品决策（B1 / A5 / B4）+ 量化实测结果（§6.6）+ 消解 §9-A1 / A3 / A5*
*本文件未改动任何代码。*
