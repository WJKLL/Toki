# 边缘质量方案（v1.54 提案）

> 前置：`PLAN_components_v1.53.md`（组件系统，期 1 已落地）
> 状态：**待确认** —— 本方案含一条明确的收尾线，完成后即回主线
> 编号说明：落地时按 `PROJECT_SPEC.md` 分配

---

## 0. 结论摘要

**用引导滤波（Guided Filter）把"边缘判断"从「猜」变成「跟着图像走」。做完就收，回主线。**

理由一句话：**我们一直在用深度值和 mask 概率去猜物体边界在哪；而物体边界本来就画在图像上 —— 亮度/颜色在那里有明显跳变。**

---

## 1. 先说偏离这件事

**主线是什么**：P-24 空间壁纸的**组件系统**（时钟 / 景深 / 导出 / 模板）。期 1（组件模型 + 数字时钟 + 3D 透视）已落地。

**为什么陷进来**：组件要"叠在分层画面上"，于是分层的质量变成了前置条件。而分层的边缘问题一环扣一环，每修一处就冒出下一处。

**已经真实产出的东西**（不是白做）：

| 资产 | 价值 |
|:--|:--|
| **主体遮罩预览 / 层素材预览** | 让"看不到的问题"变成可见 —— 这是这轮最有价值的产出 |
| S-39 主体分割服务 | 人像/插画双模型 + 自动调度，独立可用 |
| 深度归一化改视差域 | 实测近距动态范围 ×2.2，是净改进 |

**教训**：在没有可见性的情况下靠推理调参，代价极高。预览开关应该更早做出来。

---

## 2. 根因（同意参考文档的核心判断）

**单目深度模型输出的是平滑的深度图。** 训练损失对大误差敏感、对高频边缘不敏感，于是物体边界处深度是"渐变过渡"而不是"台阶"。

**后果正好解释了我们遇到的全部现象**：

| 现象 | 解释 |
|:--|:--|
| 身体被分到背景 | 边界处深度渐变，阈值切在渐变带里 |
| 背景被收纳 | 渐变带里深度"看起来连续"，连通性走得通 |
| 手指被误剔 / 又被保留 | 同一条渐变带，两种判据给出相反结论 |
| 换参数总在两者间摇摆 | **因为判据本身选错了** |

**关键结论：不要再用深度值/概率去猜边界。**

---

## 3. 对参考文档的逐条评估

| 建议 | 采纳 | 理由 |
|:--|:--:|:--|
| **引导滤波（Guided Filter）** | ✅ **采纳，作为核心** | 唯一"用对信息"的方案；O(n) 可纯 Dart 实现；直接解决边缘对齐 |
| Shader 边缘混合 | ✅ 采纳（第 2 步） | 已在用 shader，成本低 |
| 升级到 Depth Anything V2 Small | ⚠️ **暂缓** | 有价值，但 +25MB（int8）且 ViT 架构在手机 CPU 上更慢；**先做引导滤波，不够再评估** |
| 原生 OpenCV 实现 | ❌ **不采纳** | **必须移植鸿蒙** —— 原生实现等于堵死镜像路线。引导滤波是 O(n) 算法，纯 Dart 完全够快 |
| SEGF / MD2E 训练侧改动 | ❌ 不采纳 | 需要重新训练，成本远超收益 |
| 场景分工（建筑/风景/人物） | ✅ 概念采纳 | 见 §5：判据与场景无关，只有"主体定义"与场景有关 |

---

## 4. 方案（三步，每步都有验收）

### 步骤 1：引导滤波（核心，~150 行 Dart）

**做什么**：以**原图（RGB→亮度）为引导**，对**深度图**做边缘保持平滑。

**原理**（标准快速引导滤波，O(n)）：

```
meanI  = box(I, r)          corrI  = box(I*I, r)
meanP  = box(p, r)          corrIP = box(I*p, r)
varI   = corrI  - meanI*meanI
covIP  = corrIP - meanI*meanP
a = covIP / (varI + eps)
b = meanP - a*meanI
q = box(a, r) * I + box(b, r)
```

- 引导图中**有明显边缘**（物体边界）的地方 → `varI` 大 → `a` 小 → **深度不被平均掉，边缘被保留** ✅
- 引导图**平坦**的地方 → 深度被平滑 → 去掉噪声 ✅

**参数**：
- 半径 `r` = 短边的 3~5%（1440 → 约 50px）
- `eps` = `1e-4` 量级（对归一化深度）
- **在低分辨率（长边 512）上做，再上采样** —— 快十几倍，且边缘位置由原图决定、不受影响

**验收**：打开「深度图预览」，**人物/建筑轮廓处的深度应当出现明显"台阶"**，而不是渐变带。

**这一条如果成了，后面几条可以不做。**

### 步骤 2：mask 边缘对齐（~40 行）

**做什么**：把同一套引导滤波用到 **S-39 的主体 mask** 上。

**为什么**：mask 是模型输出的软概率，边缘同样是糊的。引导滤波后，mask 边缘会**贴合图像上的真实物体轮廓**。

**收益**：现在那一堆启发式（闭运算 / 连通性 / 深度下限）**可以删掉或降级为兜底** —— 因为 mask 本身变准了。

### 步骤 3：收尾 + 简化

- **删掉** `_keepConnected` / `_depthFloor` 的串联（保留代码但不再默认启用）
- **保留**两套预览开关（它们是这轮唯一不可替代的产出）
- **固化**一组默认参数，不再开放给用户调

---

## 5. 明确不做的事

| 不做 | 原因 |
|:--|:--|
| ❌ 原生 OpenCV | 鸿蒙移植 |
| ❌ 换深度模型（现阶段） | 体积/速度代价大，且**根因是"用错判据"而非模型不够好** |
| ❌ 训练侧改动（MD2E/SEGF） | 成本远超收益 |
| ❌ 继续加"深度阈值 / 连通性"类启发式 | **已经证明这条路是在两者之间摇摆** |
| ❌ 让用户调边缘参数 | 用户不该为这类问题负责 |

---

## 6. 收尾线（重要）

**这个方案做完就收，不再无限迭代。** 具体标准：

1. 引导滤波落地并实机验证
2. 若验收通过 → **把默认参数固化，收工**
3. 若验收不通过 → **最多再做一轮参数调整**；仍不行则**记录为已知限制**，回主线

**理由**：边缘质量是"完美主义陷阱"。当前效果（底部干净、手指完整）已经达到**可用**水平；再往上投入的边际收益远低于把组件系统做完。

---

## 7. 回到主线

边缘这条线收尾后，主线是：

| 期 | 内容 | 状态 |
|:--|:--|:--|
| 1 | 组件模型 + 数字时钟 + Z/视差解耦 + 3D 透视 | ✅ 已落地 |
| **2** | **玻璃质感（复用 `LensRefraction`）+ 景深虚化** | ← **下一步** |
| 3 | 静态图导出 | |
| 4 | 模板体系 | |
| 5 | 轨迹录制 + 离屏渲染 + GIF 导出 | |

**建议：引导滤波（步骤 1）落地后，无论结果如何都转期 2。**

---

## 8. 请确认

| # | 问题 | 我的建议 |
|:--|:--|:--|
| **A** | 是否按此方案做引导滤波？ | ✅ 是 |
| **B** | 做完后是否**无论结果都回主线**？（最多再调一轮参数） | ✅ 是 |
| **C** | Depth Anything V2 是否暂缓？ | ✅ 暂缓 |
| **D** | 是否保留两套预览开关为常驻调试功能？ | ✅ 保留（本次唯一不可替代的产出） |
